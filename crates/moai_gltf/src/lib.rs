/*!
    Moved from .obj loading to gltf (even though gltf loading is much slower)
    
    This was because the model loading should be up to the user since moai
    is only to be a renderer. This package 'moai_gltf' is for simple loading
    for those who don't care about writing their own loader.
*/
